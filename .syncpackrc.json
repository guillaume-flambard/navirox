{
  "semverGroups": [
    {
      "label": "Symbiote packages are pinned exactly. Their version lines move independently and a floating range has already been shown to break pod install upstream.",
      "range": "",
      "dependencies": ["@symbiote-native/**"]
    },
    {
      "label": "TypeScript stays on the 6.x line: typescript-eslint@8 peers a version below 6.1.0, so the TypeScript 7 line is not usable yet.",
      "range": "~",
      "dependencies": ["typescript"]
    },
    {
      "label": "Caret ranges for everything else. Must stay last: syncpack applies the first matching group, so a catch-all placed first would shadow the rules above.",
      "range": "^",
      "dependencies": ["**"]
    }
  ],
  "versionGroups": [
    {
      "label": "The renderer and framework peers are floors, not caret ranges. A caret on a 0.x version is bounded to that minor, so `^0.86` would exclude react-native 0.87 and put the adapter back in the floating-range failure mode the exact-pin rule exists to prevent. Both spellings match upstream's own peer declarations.",
      "dependencies": ["react-native", "vue"],
      "dependencyTypes": ["peer"],
      "isIgnored": true
    }
  ]
}
