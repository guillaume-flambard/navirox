## 1. Record the benchmark input and complete the contract

- [x] 1.1 Re-run the read-only pinned benchmark and record its exact revision, report path and counters in the workflow record's source provenance. Verify `pnpm test:benchmarks -- --project baserow` exits 0 and the record names the revision pinned in `benchmarks/catalog.json`.

  `node scripts/benchmark-projects.mjs --project baserow` exits 0 and reports `baserow: nuxt, 46 routes, 41 screens`, writes `docs/evidence/vue-target-baserow-diagnostic.json` and reports `12 screens compiled, 113 findings (unsupported-directive 20, unsupported-element 41, unsupported-text 52), no file written`. The record `docs/evidence/workflow-baserow-field-work.md` names the immutable revision `81e094a1f4b3a62625c218d78fe319ba44098617` pinned in `benchmarks/catalog.json`, the report path, and those counters.

- [x] 1.2 Add the ordered actions, the minimum data each action reads or writes, and the failure state to the workflow record and to the template in `docs/WORKFLOW-EVIDENCE.md`. Verify the template lists the three fields and `pnpm prettier --check docs/WORKFLOW-EVIDENCE.md docs/evidence/workflow-baserow-field-work.md` exits 0.

  `docs/WORKFLOW-EVIDENCE.md` gained a `## Workflow contract` section between `## Trigger` and `## Success state` listing the ordered actions, the minimum data per action and the failure state. `docs/evidence/workflow-baserow-field-work.md` carries the concrete contract (six ordered actions, a minimum-data table with columns Action / Reads / Writes, and the visible failure state the declared actions can reach: saving without a status reports that the change was not saved). `pnpm prettier --check docs/WORKFLOW-EVIDENCE.md docs/evidence/workflow-baserow-field-work.md` prints `All matched files use Prettier code style!`.

## 2. Create the original fixture, data and asset

- [x] 2.1 Add the workflow's web source under `packages/target-vue/fixtures/field-workflow/`, covering the declared actions and a failure state the declared actions can reach. Verify `pnpm --filter @memolabs-apps/target-vue test` passes and the source compiles with no findings.

  `packages/target-vue/fixtures/field-workflow/FieldWorkflowScreen.web.vue` declares no credentials and no network call: selecting a record fills the detail panel, the status and note cycle through a fixed set, the attachment toggles, saving with a status reports `Saved`, and saving after clearing the status reaches the visible failure state. The source compiles with zero findings (`findings: []`, `hasCode: true`) and `pnpm --filter @memolabs-apps/target-vue test` passes 16 tests in 2 files.

- [x] 2.2 Generate the emitted native file and its provenance manifest with the existing compiler path and check them in beside the source. Verify a fixture test recompiles the source and the emitted file matches byte for byte with a stable manifest hash.

  `FieldWorkflowScreen.native.vue` and `FieldWorkflowScreen.provenance.json` were generated by `compileVueTarget` with filename `FieldWorkflowScreen.web.vue` and outputPath `packages/target-vue/fixtures/field-workflow/FieldWorkflowScreen.native.vue`; the manifest hash is `9721b767172a871f915bfca7c4714ff38d52cb354030cc6cf8c0003846fcf46a` at compiler `0.1.1`. `src/fixtures.test.ts` gained a `field workflow fixture` block whose first test recompiles the source, asserts no findings, byte equality with the emitted file, a stable hash across two compilations and the committed manifest JSON. The generated native file joined `.prettierignore` beside the records one for the same reason.

- [x] 2.3 Add the synthetic data module and the original asset beside the fixture. Verify a test asserts the data is the declared fixed set and that no fixture file references an external asset source.

  `fieldRecords.ts` carries the invented fixed set and `site-photo.svg` is a hand-drawn asset created for this project with no external reference. The new tests `carries only invented data and its own asset` (asserts the two records, the status and note lists and the attachment name and label, and that the asset exists) and `references no external asset or service` (walks the source, the emitted file, the manifest, the data module and the asset and asserts none contains `src="http`, `url(http`, `fetch(` or `XMLHttpRequest`) pass.

## 3. Add the acceptance scenario

- [x] 3.1 Write the acceptance scenario for the new fixture naming its ordered actions, capture moments and identifiers, validated with `validateScenario`. Verify a test asserts validation returns no issues and that the scenario's action names and identifiers match the workflow record.

  `packages/visual-benchmark/src/scenarios/field-workflow.ts` exports `fieldWorkflowScenario` (name `field-workflow`, route `/field-work`, five captures `rest` / `first-meaningful` / `midpoint` / `settled` / `interrupted` in that order, motion declared interruptible, tolerance `gridSize: 0.05` with `FIELD_WORKFLOW_IDENTIFIERS` as its identifiers) and `FIELD_WORKFLOW_IDENTIFIERS`, both re-exported from `src/index.ts`. `src/scenarios.test.ts` asserts `validateScenario` returns `[]` and that every action press the captures declare and every declared identifier appears in `docs/evidence/workflow-baserow-field-work.md`; the record gained the sentence naming `packages/visual-benchmark/src/scenarios/field-workflow.ts`, the five drive identifiers and the four capture identifiers, and the template in `docs/WORKFLOW-EVIDENCE.md` gained the matching `Drive identifiers` bullet. `packages/visual-benchmark/harness/web/main.ts` now selects the fixture by path (`/records` and `/field-work`), and a real headless Chrome dump of `http://127.0.0.1:5211/field-work` against the vite harness returns a DOM containing `field-screen`, `field-list`, `field-row` and `field-select`. `pnpm --filter @memolabs-apps/visual-benchmark test` passes 81 tests in 8 files.
- [x] 3.2 Parameterise `scripts/lib/fixture-app.mjs` so a caller names its fixture, application name and identifiers, with the records caller passing its current values. Verify `node scripts/verify-records-screen.mjs --keep` exits 0 with the same manifest hash, single-runtime assertion and bundle sizes.

  `scripts/lib/fixture-app.mjs` now exports a `RECORDS_FIXTURE` record (app name, web fixture, emitted screen, output path, source name, bundle name and test identifiers) and `scaffold`, `installGeneratedScreen`, `bundle` and `assertTestIds` take it as an optional argument, so a second fixture names its own values instead of the helpers growing a copy. `scripts/verify-records-screen.mjs` and `packages/visual-benchmark/scripts/capture-records-native.mjs` pass `RECORDS_FIXTURE` explicitly. `node scripts/verify-records-screen.mjs --keep` exits 0 with the unchanged numbers: 32 artifacts, 81 scaffolded files, manifest `e18712f368212b2481a868a3e6fa3fd5e42a58449fc099e1f503648b7eab4c48` at compiler 0.1.1, 5 packages inside the app, 5 singletons one copy each, iOS bundle 6576 kB and Android 6597 kB, 9/9 test identifiers in each bundle.

## 4. Record provenance and close

- [x] 4.1 Record the provenance and non-affiliation language in `docs/pilots/baserow.md` and `docs/evidence/workflow-baserow-field-work.md`, stating the workflow is this project's hypothesis and not the benchmark project's workflow. Verify both documents name the immutable revision and state the non-affiliation boundary, and `pnpm prettier --check` passes.

  `docs/pilots/baserow.md` and `docs/evidence/workflow-baserow-field-work.md` each name the immutable revision `81e094a1f4b3a62625c218d78fe319ba44098617` and now state that the workflow is this project's own hypothesis drawn from the reproducible analysis, not Baserow's chosen workflow, not a statement about Baserow's product direction and not customer demand, and that Baserow has not requested, reviewed or endorsed it and Navirox has no affiliation with Baserow. `pnpm prettier --check docs/pilots/baserow.md docs/evidence/workflow-baserow-field-work.md` prints `All matched files use Prettier code style!`.
- [x] 4.2 Run `pnpm build`, `pnpm typecheck`, `pnpm test`, `pnpm lint`, `pnpm format:check`, `pnpm deps:check` and `pnpm test:benchmarks -- --project baserow`. Verify every command exits 0.

  All seven exit 0: `pnpm build`, `pnpm typecheck`, `pnpm test`, `pnpm lint`, `pnpm format:check` and `pnpm deps:check` (logs `/tmp/p1-<name>.log`), and `node scripts/benchmark-projects.mjs --project baserow` reporting `46 routes, 41 screens` and `12 screens compiled, 113 findings (unsupported-directive 20, unsupported-element 41, unsupported-text 52), no file written`.
- [x] 4.3 Run `openspec validate vue-benchmark-workflow-contract --strict` before implementation and before archival, then archive the change. Verify no task is marked complete until its stated command or artifact exists.

  `openspec validate vue-benchmark-workflow-contract --strict` printed `Change 'vue-benchmark-workflow-contract' is valid` (exit 0) before implementation and again before archival, and `openspec archive vue-benchmark-workflow-contract --yes` archived the change as `2026-09-22-vue-benchmark-workflow-contract`, applying its delta to `openspec/specs/workflow-value-evidence/spec.md` (+ 1 added, ~ 1 modified). Every task was left unchecked until its stated command or artifact existed; the archive reported 9 of 10 tasks because this task is the archival itself.
