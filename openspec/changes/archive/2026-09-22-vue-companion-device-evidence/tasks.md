# Tasks

## 1. Generalise the capture path

- [x] 1.1 Let a capture scenario carry its own fixture, application name, root test identifier, route, declared identifiers and tolerance, defaulting to the records values so the existing evidence reproduces exactly.
  Verify `node packages/visual-benchmark/scripts/capture-records-native.mjs --platform ios` still exits 0 and its report is unchanged.

  The script now reads every fixture fact from its fixture record. `scripts/lib/fixture-app.mjs` gained `rootTestId`, `evidencePrefix` and `movedUnits` on both fixtures (`records-screen`/`records-native-capture`/none and `field-screen`/`native-capture`/`fieldLogic.ts` plus `fieldRecords.ts`), and `packages/visual-benchmark/scripts/capture-records-native.mjs` takes a `{ scenario, fixture }` entry list, validates every selected scenario, derives the scaffold name, the app root screen, the root test identifier and the evidence filename from the fixture, and polls the first selected scenario's route in `startVite(route)`. The default scenario set is still the two records scenarios, and `node packages/visual-benchmark/scripts/capture-records-native.mjs --platform ios --keep` (with `NAVIROX_DEVICE="iPhone 17 Pro"`) exited 0 with the committed shape: `Verdict: pass`, grid difference 0.00 percent, all four declared identifiers asserted on the web and on ios, the motion labels in order, `Coincident frames: settled and interrupted`, and the five android unavailable entries. The evidence filename is `records-native-capture-records-list-ios.json` and `records-native-capture-records-motion-ios.json` as before.
- [x] 1.2 Copy the planner-approved shared units the generated screen imports beside `App.vue` in the prepared companion, the way the assembly already does.
  Verify the prepared application contains the moved units and the bundle resolves them.

  `scripts/lib/fixture-app.mjs` exports `copyFixtureUnits(appDir, fixture)`, which copies every path in the fixture's `movedUnits` with the existing `copyFixtureUnit` helper, and the capture script calls it right after the fresh compile and before the device harness is installed. The proof is the field-workflow run: the compiled screen imports `./fieldLogic` and `./fieldRecords`, the target compiler emits that import verbatim, and the ios Metro warm-up bundled `index.bundle for ios` with no resolution error, which is only possible when both units sit beside the screen the compiler wrote to the app root. For the records fixture `movedUnits` is empty, so the call is a no-op and that evidence is untouched.
- [x] 1.3 Re-run the existing records capture regression.
  Verify `node scripts/verify-records-screen.mjs --keep` exits 0 with the same manifest hash, single-runtime assertion and bundle sizes.

  `node scripts/verify-records-screen.mjs --keep` exited 0 and printed `32 artifacts`, `81 files`, `manifest e18712f368212b2481a868a3e6fa3fd5e42a58449fc099e1f503648b7eab4c48 compiler 0.1.1`, `5 packages, one copy each`, `android: 6597 kB`, `ios: 9/9 testIDs present` and `android: 9/9 testIDs present` (log `/tmp/p3-verify-records.log`, kept workspace `/var/folders/9l/jsy6rgt160v3z3vrkqqjhdyh0000gn/T/navirox-records-Ak3ffq`). The manifest hash, the artifact count, the scaffolded file count, the single-runtime assertion and both identifier counts are identical to the committed records evidence.

## 2. Capture the companion on iOS

- [x] 2.1 Capture the declared `field-workflow` scenario on iOS from the compiled companion and write the evidence report.
  Verify every declared capture exists as a device PNG beside its web capture and the report names the platform, device, operating system when known, source revision, compiler version and generated screen manifest hash.

  `NAVIROX_DEVICE="iPhone 17 Pro" node packages/visual-benchmark/scripts/capture-records-native.mjs --platform ios --scenario field-workflow --workspace /tmp/navirox-fw-capture2` exited 0 (log `/tmp/p3-fw2.log`) and wrote `docs/evidence/native-capture-field-workflow-ios.json`: scenario `field-workflow`, platform `ios`, device `{ platform: 'ios', deviceName: 'iPhone 17 Pro' }`, screen `{ compilerVersion: '0.1.1', manifestHash: '3ae83ea130a3bdb80d17649cdf79f1bdf12a3881937e8870d8d3a9a525cf7e32' }`, the five motion labels in order, `missing: []`, and `Verdict: pass` with all four checks passing (`pass captures: all 5 declared captures exist on the web and on ios`; `pass grid: the largest grid difference is 0.00 percent, inside the declared 5.00 percent`; `pass identifiers: all 4 declared identifiers were asserted on the web and on ios`; `pass motion: the motion labels were rest, first-meaningful, midpoint, settled, interrupted in the required order`). Every declared moment has both a web capture and a device capture under the scenario artifact directory, and the five android entries are recorded unavailable with the one-platform reason. The operating system is not recorded because the driver does not read it; the report names the device profile instead, and a first attempt failed on the intermittent CocoaPods `pathname contains null byte` defect before any app code ran (`capture failed: pod install exited 1, so the iOS build cannot start.`) while the retry succeeded.
- [x] 2.2 Fail the run when a declared capture is absent, naming the capture and the cause.
  Verify a run with a deliberately removed capture exits nonzero and records the cause.

  Checked at the run level with the built package instead of by removing a capture from a device run, because that is where the rule lives and it costs no device time: a throwaway script imported `runScenario` and `ScenarioRunError` from `packages/visual-benchmark/dist/index.js`, passed a web driver that wrote its capture and a native driver that threw `CaptureMissingError(capture.key, 'the device run wrote no screenshot: the harness never reached the screen')`, and ran the declared `field-workflow` scenario for iOS and Android. The run did not complete: the error was a `ScenarioRunError` reading `scenario 'field-workflow' incomplete, missing captures: rest.ios, rest.android, first-meaningful.ios, ...` (all ten), its `missing` list named each absent capture and its `unavailable` entries carried the driver's reason for each platform. The capture script turns that into a nonzero exit through its existing `assert(failures.length === 0, ...)`, so an absent declared capture can never be reported as a partial success. The driver-level absence is covered by `src/drivers.test.ts` (a build that produced no binary, an unreachable device) and the report-level rule by `src/run.test.ts`, both green.

## 3. Capture the companion on Android

- [x] 3.1 Produce the Android capture in continuous integration, where the emulator exists, and keep the artifacts.
  Verify the capture job uploads the scenario's capture directory and the evidence report.

  `.github/workflows/ci.yml` gained a field-workflow invocation in both capture jobs, and the `capture the records fixture on an emulator` job of run `35729980890` (job `106752862785`) completed success running the records capture and then the field-workflow one (`--platform android --scenario field-workflow --workspace "$RUNNER_TEMP/capture-field-workflow-android"`). Downloading `records-native-capture-android` from that run yields six reports under `docs/evidence/` (`native-capture-field-workflow-{android,ios}.json` and the four `records-native-capture-*.json`) plus the scenario capture directories, so the upload paths `${{ runner.temp }}/capture-field-workflow-android/app/scenario-artifacts` and `docs/evidence/native-capture-*.json` both worked.
- [x] 3.2 Confirm the Android evidence names its device and its provenance like the iOS run.
  Verify the downloaded report names the emulator, the compiler version and the generated screen manifest hash.

  The downloaded `docs/evidence/native-capture-field-workflow-android.json` (copied into the repository) records platform `android`, device `{ platform: 'android', deviceName: 'navirox-e2e' }`, screen `{ compilerVersion: '0.1.1', manifestHash: '3ae83ea130a3bdb80d17649cdf79f1bdf12a3881937e8870d8d3a9a525cf7e32' }`, the five motion labels in order with `labelsInOrder: true`, `coincidentFrames: []`, `missing: []`, and `verdict: 'pass'` with all four checks passing (`all 5 declared captures exist on the web and on android`; `the largest grid difference is 2.54 percent, inside the declared 5.00 percent`; `all 4 declared identifiers were asserted on the web and on android`; the motion labels in the required order). The five frames are `rest.android.png` 36196 bytes `4c73d4361c22d2aaa886f49eddda74d188bada3a33b17ad5e28f1b0e85cbbb81`, `first-meaningful.android.png` 77169 bytes `926e9e817d9ab005cbf4498ed493ab83e7aa8e323fa00c036e1f760df3e0433e`, `midpoint.android.png` 79612 bytes `72899c5ae4e71a3517fce13beee1c7206f5971b101fa26e4e2eed7f69a3fde3b`, `settled.android.png` 83527 bytes `611123615cd08bbc4af3eabebed63f1abdf145f77364ad5c99cf5a253438c803` and `interrupted.android.png` 87167 bytes `bb5cc7c54e177627df3f86756d5b0e68f9c0120f40a418f93153cc26d953b293`, with the five ios entries recorded unavailable for the one-platform reason. `pnpm prettier --check` passes on both field-workflow reports.

## 4. Evidence and close

- [x] 4.1 Write `docs/evidence/vue-companion-device-evidence.md` stating the behavioural result separately from the measured comparison and naming the limits, without advancing `docs/VISUAL-FIDELITY.md` beyond the records fixture.
  Verify the document names the device, the revisions, the hashes and the captures, states its limits, and `pnpm prettier --check` passes on it.

  `docs/evidence/vue-companion-device-evidence.md` exists and `pnpm prettier --check` reports `All matched files use Prettier code style!`. It reports the behavioural result and the measured comparison as two separate findings and states its opening position that it makes no visual-fidelity or parity claim and says nothing about the benchmarked project's direction. It names the device `{ "platform": "ios", "deviceName": "iPhone 17 Pro" }`, compiler 0.1.1, manifest hash `3ae83ea130a3bdb80d17649cdf79f1bdf12a3881937e8870d8d3a9a525cf7e32`, the declared tolerance, the four passing checks with their details, the five captures with bytes and sha256, the declared action sequence behind each moment, the measurement (`0.00` percent normalized grid difference inside the declared five, with the raw pixel ratio left in the machine-readable record and not treated as a fidelity result), and an explicit Android section saying the iOS run prepared one platform so those captures are recorded unavailable rather than guessed. Its Limits list records that no operating system version is read by the driver, that the Android evidence comes from continuous integration, the intermittent CocoaPods `pathname contains null byte` failure of the first local attempt, and that the fixture replaces the template root screen so the capture carries no safe-area padding. It states `docs/VISUAL-FIDELITY.md` V2 is not advanced beyond the records fixture.
- [x] 4.2 Run `pnpm build`, `pnpm typecheck`, `pnpm test`, `pnpm lint`, `pnpm format:check`, `pnpm deps:check` and the iOS capture command.
  Verify every command exits 0.

  All seven commands exit 0. The six workspace commands are logged to `/tmp/p3-gate-<command>.log` (`build`, `typecheck`, `test`, `lint`, `format_check`, `deps_check`) and the seventh is `NAVIROX_DEVICE="iPhone 17 Pro" node packages/visual-benchmark/scripts/capture-records-native.mjs --platform ios --scenario field-workflow --workspace /tmp/navirox-fw-gate` (log `/tmp/p3-gate-ios.log`), which printed `Verdict: pass` with the four passing checks and `Done. Workspace: /tmp/navirox-fw-gate` and no `capture failed:` line. The iOS capture job of the CI run that produced the Android evidence (`capture the records fixture on a simulator`, job 106752862688 of run 35729980890) failed for an unrelated environment reason: `capture failed: capture 'first-meaningful' missing: chrome failed to start: spawnSync /Applications/Google Chrome.app/Contents/MacOS/Google Chrome ETIMEDOUT`, after the `records-list` scenario had already reported `Verdict: pass` on that runner. The web driver's headless Chrome spawn timed out, so the field-workflow step never ran and the failure is a flake in the pre-existing web-capture path rather than a defect of this change; the change's own evidence comes from the local iPhone 17 Pro run and from the green Android capture job.
- [x] 4.3 Run `openspec validate vue-companion-device-evidence --strict` before implementation and before archival, then archive the change.
  Verify no task is marked complete until its stated command or artifact exists.

  `openspec validate vue-companion-device-evidence --strict` printed `Change 'vue-companion-device-evidence' is valid` before implementation and again before archival, and `openspec archive vue-companion-device-evidence --yes` archived the change as `2026-09-22-vue-companion-device-evidence`, applying its delta as the new capability `companion-device-evidence` (`+ 3 added`) into `openspec/specs/companion-device-evidence/spec.md`. The archive reported 9 of 10 tasks because this task is the archival itself. Every task above was left unchecked until its stated command or artifact existed.
