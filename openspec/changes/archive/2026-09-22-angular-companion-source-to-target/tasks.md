## 1. Record the seam the companion consumes

- [x] 1.1 Record the seam decision: the companion consumes the planner decision that classifies `src/app/record-workflow.data.ts` as `shared` by `unit-shared-logic`, the screen is a named manual native replacement, and Angular imports stay inside `source-angular`. Verify the provenance record names the consumed subject with its classification and rule, marks the screen manual with its reason, and `pnpm prettier --check` exits 0 on it.

  The decision is recorded in `docs/evidence/angular-companion.provenance.json`: the consumed subject is `angular:src/app/record-workflow.data.ts:utility:default`, copied into the companion as the moved entry `record-workflow.data.ts` with the approving decision `shared` and the source evidence `src/app/record-workflow.data.ts`, and the screen `App.vue` is a `manual` entry whose reason states that the journey has no target compiler. The prose record `docs/evidence/angular-companion-assembly.md` repeats it in its `## The seam the companion consumes` and `## Manual native work` sections, and states that Angular imports stay inside `source-angular` (the companion imports nothing from Angular, and `packages/source/src/boundary.test.ts` scans every adapter package for target-provider specifiers). `pnpm prettier --check docs/evidence/angular-companion.provenance.json docs/evidence/angular-companion-assembly.md` prints `All matched files use Prettier code style!`.

- [x] 1.2 Add the provenance record beside the seam proof as the machine-readable companion record. Verify it parses, carries no `generated` entry, and names the immutable fixture and the benchmark revision.

  `docs/evidence/angular-companion.provenance.json` is written by the assembly command, so it cannot drift from the code that produces it. It parses as JSON, carries three entries (one `moved`, two `manual`) and no `generated` entry, and names the fixture `packages/source-angular/fixtures/record-workflow`. The benchmark revision and the workflow it came from are named in `docs/evidence/angular-companion-assembly.md` and in `docs/evidence/workflow-suitecrm-record-workflow.md` (`2cd77380bc838b8bd6c80f9fbe25855d73ef860c`), because a companion record describes a companion and the benchmark belongs to the workflow record it implements. The record's `compilerVersion` and `manifestHash` are both `none`, which is the honest value for a journey with no target compiler, and `packages/visual-benchmark/src/companion.test.ts` asserts exactly that.

## 2. Assemble the companion

- [x] 2.1 Add the hand-written screen beside the fixture, rendering the workflow's declared identifiers and importing the shared module. Verify `pnpm --filter @memolabs-apps/source-angular test` passes and the fixture still reads with no finding.

  `packages/source-angular/companion/App.vue` is the hand-written screen: it imports `./record-workflow.data` (the module the assembly copies to the application root), calls `nextIn` and `saveOutcome`, and renders every declared identifier (`record-workflow-screen`, `-queue`, `-row`, `-select`, `-detail`, `-field`, `-status`, `-attach`, `-attachment`, `-save`, `-saved`, `-error`) as a static `testID`. It deliberately sits outside `fixtures/record-workflow/` so the fixture reading stays exactly 5 files / 3 units / 1 capability with no finding, which `node packages/navirox/dist/bin.js analyze packages/source-angular/fixtures/record-workflow --json` still reports and which the assembly asserts. `pnpm --filter @memolabs-apps/source-angular test` passes with 4 files / 37 tests.

- [x] 2.2 Add the assembly command: run the read-only analysis and plan on the fixture, assert the three planner decisions, scaffold the application, copy the approved shared module unchanged, write the screen as the app root, and write the provenance record. Verify the command exits 0 and prints the record path.

  `scripts/build-angular-companion.mjs` does all of that and exits 0, printing `Done. Provenance: /Users/memo/projects/active/apps/mobile/navirox/docs/evidence/angular-companion.provenance.json`. Its output shows the fixture read as `5 files, 3 units, 1 capabilities, 0 findings`, the three asserted decisions (`angular:src/app/record-workflow.data.ts:utility:default -> shared`, `angular:src/app/record-workflow.component.ts:component:default -> native-replacement`, `angular:src/app/record-workflow.component.ts:capability:file-reading:read -> manual`), the byte-identical copy (`moved record-workflow.data.ts (shared)`), the screen written as the app root, the provenance record written, the single-runtime assertion, and both bundles. `scripts/lib/fixture-app.mjs` gained `ANGULAR_COMPANION_FIXTURE` and a `fixtureDirectory` that prefers a declared directory, so a fixture with no compiled screen can be analysed and planned.

- [x] 2.3 Add a test that the record's moved entry carries the approving decision and a content hash equal to the fixture file's digest, and that every entry carries an origin and a reason. Verify the test fails when the moved module is edited and passes when restored.

  `packages/visual-benchmark/src/companion.test.ts` gained `describe('the assembled Angular companion', ...)` with five tests: it rebuilds through `buildCompanionProvenance`, names a hand-written screen and marks nothing generated, moves only the unit a planner decision approved, carries the bytes the record says it moved (the digest of the file at each moved entry's `sourcePath` equals its recorded `sha256`), and records that no compiler produced this screen. `pnpm --filter @memolabs-apps/visual-benchmark test` passes 9 files / 103 tests. The literal mutation check: appending a comment line to `packages/source-angular/fixtures/record-workflow/src/app/record-workflow.data.ts` produced `Test Files 1 failed | 8 passed (9)` / `Tests 1 failed | 102 passed (103)` with the failure inside `carries the bytes the record says it moved`, and restoring the file returned 103 passing with an empty `git diff --stat`.

## 3. Build and test the companion

- [x] 3.1 Add deterministic behaviour tests over the moved rules (`nextIn`, `canSave`, `saveOutcome`). Verify the test passes and fails when a rule is inverted.

  `packages/source-angular/src/record-workflow-logic.test.ts` tests the moved rules exactly where the companion copies them from, with five tests (advance, wrap, unknown current value, refuse a save without a field value, allow a save with one). `pnpm --filter @memolabs-apps/source-angular test` passes 4 files / 37 tests. The literal mutation check: replacing `field.trim().length > 0` with `field.trim().length === 0` in the shared module produced `Test Files 1 failed | 3 passed (4)` / `Tests 2 failed | 35 passed (37)` - exactly the two save tests - and restoring the file returned 37 passing with an empty `git diff --stat`.

- [x] 3.2 Bundle the assembled companion for both declared platforms and assert the declared identifiers appear in each bundle. Verify the command prints both bundle sizes and reports the identifiers found in each.

  The assembly command bundles both platforms and prints `ios: 6584 kB` and `android: 6605 kB`, then `ios: 12/12 testIDs present` and `android: 12/12 testIDs present`, so all twelve declared identifiers appear in each bundle and the copied module resolved (a missing `./record-workflow.data` would have failed the bundle).

## 4. Evidence and close

- [x] 4.1 Write `docs/evidence/angular-companion-assembly.md` naming the seam decision, every file origin with its reason, the recorded content hash, the bundle results, and the limits (no Angular target compiler, template to native screen generation not demonstrated, SuiteCRM routing and version uncertainty kept). Verify the document names each origin and `pnpm prettier --check` passes on it.

  `docs/evidence/angular-companion-assembly.md` states in its opening that nothing was generated from Angular templates and that the screen is hand-written work the record names as such, and its tables name the consumed unit, the three planner decisions, every file origin with its reason and the content hash `12e2933bdf871127f0d625614fe8bd65ae8f43fb54d620588739f18ca9c6a94d`, and the two bundle results with the twelve identifiers. Its `## Limits` section records that template to native screen generation is not demonstrated and no Angular target compiler exists, that the companion is not driven on a device by this change, that the SuiteCRM routing could not be read so no route, screen or unit is claimed, that `@angular/core` 18.2.14 is outside the tested `^20`/`^21` range so the version stays untested, and that the workflow is an unvalidated hypothesis with no affiliation. `pnpm prettier --check docs/evidence/angular-companion-assembly.md` prints `All matched files use Prettier code style!`.

- [x] 4.2 Run `pnpm build`, `pnpm typecheck`, `pnpm test`, `pnpm lint`, `pnpm format:check`, `pnpm deps:check` and the assembly command. Verify every command exits 0.

  All seven exit 0: `pnpm build`, `pnpm typecheck`, `pnpm test`, `pnpm lint`, `pnpm format:check` (`All matched files use Prettier code style!`) and `pnpm deps:check` (`✓ No issues found`), logged to `/tmp/p5-gate-<name>.log`, plus `node scripts/build-angular-companion.mjs`, logged to `/tmp/p5-gate-assembly.log`, which exited 0 (`assembly=0`) with no `angular companion assembly failed:` line and reprinted both bundle results.

- [x] 4.3 Run `openspec validate angular-companion-source-to-target --strict` before implementation and before archival, then archive the change. Verify no task is marked complete until its stated command or artifact exists.

  `openspec validate angular-companion-source-to-target --strict` printed `Change 'angular-companion-source-to-target' is valid` before implementation and again before archival, and `openspec archive angular-companion-source-to-target --yes` archived the change as `2026-09-22-angular-companion-source-to-target`, applying its delta to `openspec/specs/companion-provenance/spec.md` as `~ 2 modified`: the assembly requirement now distinguishes a journey whose target compiler exists from one whose target compiler does not, and the build requirement bundles from the generated output or from the recorded manual screen. The archive reported 9 of 10 tasks because this task is the archival itself, and every task was left unchecked until its stated command or artifact existed.
