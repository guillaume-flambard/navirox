# Tasks

## 1. Ground the fixture for analysis

- [x] 1.1 Give the field-workflow fixture a manifest so the Vue source adapter detects it, and record that no subset extension is needed. Verify `node packages/navirox/dist/bin.js analyze packages/target-vue/fixtures/field-workflow --json` exits 0 and names `adapterId` `vue` with zero findings.

  `packages/target-vue/fixtures/field-workflow/package.json` was added (`fixture-field-workflow`, private, type module, dependency `vue ^3.5.43`); the path is deliberately not a workspace member because the pnpm globs are `packages/*` and `examples/*`, matching the other fixture manifests. `node packages/navirox/dist/bin.js analyze packages/target-vue/fixtures/field-workflow --json` exits 0 with `adapterId` `vue` and summary `{"files":6,"units":3,"capabilities":0,"dependencies":1,"routes":0,"screens":0,"findings":{"info":0,"warning":0,"error":0}}`; the units are `fieldRecords.ts` (utility), `FieldWorkflowScreen.native.vue` (component) and `FieldWorkflowScreen.web.vue` (component). No subset extension is needed: the declared fixture already compiles with zero findings.

- [x] 1.2 Move the workflow rules into a shared logic module the fixture screen imports, regenerate the emitted native file and its provenance manifest, and keep the byte-for-byte fixture test passing. Verify `pnpm --filter @memolabs-apps/target-vue test` passes with the new manifest hash recorded.

  `packages/target-vue/fixtures/field-workflow/fieldLogic.ts` now holds the pure workflow rules (`nextIn`, `canSave`, `saveOutcome`) with no framework import, and `FieldWorkflowScreen.web.vue` imports them instead of defining `nextIn` locally (`save()` now calls `saveOutcome(status.value)`). The emitted `FieldWorkflowScreen.native.vue` and `FieldWorkflowScreen.provenance.json` were regenerated through the existing compiler path: zero findings and the new manifest hash `3ae83ea130a3bdb80d17649cdf79f1bdf12a3881937e8870d8d3a9a525cf7e32` (the P1-era hash `9721b767...` is stale). `pnpm --filter @memolabs-apps/target-vue test` passes `Test Files 2 passed (2)` / `Tests 16 passed (16)`, which includes the byte-for-byte comparison against the committed native file, the stable-hash assertion and the committed-manifest-JSON assertion.

## 2. The provenance builder

- [x] 2.1 Add the companion provenance builder to the verification package with the origin types, a record builder that requires every file to carry an origin and a reason, and a check that refuses a generated entry whose fresh compilation differs. Verify `pnpm --filter @memolabs-apps/visual-benchmark test` passes, and that a generated entry with a mismatching fresh output throws.

  `packages/visual-benchmark/src/companion.ts` exports `CompanionFileOrigin` (`generated` | `moved` | `manual`), `CompanionFileEntry` (path, origin, reason, and for a moved entry the approving planner `decision` and the `sourcePath`), `CompanionProvenance` (schemaVersion, fixture, compilerVersion, manifestHash, screen, files), `buildCompanionProvenance` (refuses an empty file list, a duplicate path, an unknown origin, an entry with no reason, a moved entry with no approving planner decision, and a screen that is not named as generated, so a hand-written screen cannot pass as compiler output), `serializeCompanionProvenance`, and `refreshCompanionScreen` which compiles the fixture again and, through the existing fixture rule, throws `StaleFixtureScreenError` when a generated screen's fresh output differs. `packages/visual-benchmark/src/companion.test.ts` (9 tests) asserts the recorded origins and reasons, each refusal, the stable serialization, an accepted fresh screen, a drifted screen rejected with `StaleFixtureScreenError`, and a record naming no generated file at the emitted path rejected with `CompanionProvenanceError`. `pnpm --filter @memolabs-apps/visual-benchmark test` passes `Test Files 9 passed (9)` / `Tests 90 passed (90)`.

- [x] 2.2 Export the builder from the package entry point. Verify `pnpm --filter @memolabs-apps/visual-benchmark build` exits 0.

  `packages/visual-benchmark/src/index.ts` re-exports `COMPANION_PROVENANCE_SCHEMA_VERSION`, `CompanionProvenanceError`, `buildCompanionProvenance`, `refreshCompanionScreen`, `serializeCompanionProvenance` and the types `CompanionFileEntry`, `CompanionFileOrigin`, `CompanionProvenance`, `CompanionProvenanceInput` and `CompanionScreenRefreshOptions` from `./companion.js`. `pnpm --filter @memolabs-apps/visual-benchmark build` (tsc --build) exits 0 and `pnpm prettier --write` leaves the three touched files clean.

## 3. Assemble the companion

- [x] 3.1 Add the field-workflow fixture record to `scripts/lib/fixture-app.mjs` and write the assembly script that runs the read-only analysis and plan, asserts the planner classification of every moved unit, scaffolds the application, compiles the fixture fresh, writes the generated screen, copies the approved shared units unchanged and writes the provenance record. Verify the script exits 0 and prints the provenance path.

  `scripts/lib/fixture-app.mjs` gained `FIELD_WORKFLOW_FIXTURE` plus the `LAUNCHER` constant and the `fixtureDirectory`, `analyzeFixture`, `planFixture`, `decisionFor` and `copyFixtureUnit` helpers, all parameterised so a caller names its own fixture. `scripts/build-field-workflow-companion.mjs` reads the fixture with the real adapter (7 files, 4 units, 0 findings) and the real planner (`vue:fieldLogic.ts:utility:default -> shared`, `vue:fieldRecords.ts:utility:default -> shared`, both screens `native-replacement`), refuses anything reclassified, scaffolds `field-workflow-app`, compiles the fixture fresh (manifest `3ae83ea130a3bdb80d17649cdf79f1bdf12a3881937e8870d8d3a9a525cf7e32`, compiler 0.1.1), copies the two shared units and asserts each copy is byte-identical to its source, writes `docs/evidence/vue-companion-assembly.provenance.json` (schemaVersion 1, four file entries: the generated screen, the two moved units with their approving decision, source path and content hash, and `index.js` as the manual scaffolder bootstrap), re-checks the recorded screen against a fresh compilation, then bundles both platforms. The run exits 0 printing `ios: 6584 kB` and `android: 6604 kB` with `15/15 testIDs present` on each and `Done. Provenance: /Users/memo/projects/active/apps/mobile/navirox/docs/evidence/vue-companion-assembly.provenance.json` (log `/tmp/p2-assembly.log`, kept workspace `/var/folders/9l/jsy6rgt160v3z3vrkqqjhdyh0000gn/T/navirox-companion-7vioi6`).

- [x] 3.2 Extend the provenance test to cover the assembled record: every file carries an origin, every moved entry names the planner decision that approved it, every manual entry names its reason, and each moved copy is byte-identical to its fixture source. Verify the test fails when a moved copy is edited and passes when it is restored.

  `packages/visual-benchmark/src/companion.test.ts` gained a `describe('the assembled companion')` block that reads the committed record from `docs/evidence/vue-companion-assembly.provenance.json` and asserts it rebuilds through `buildCompanionProvenance` unchanged, every file carries an origin and a reason, every moved entry names an approving `shared` or `portable` decision plus a sha256 equal to the digest of the file at its `sourcePath`, the screen is the generated entry, and every manual file names a reason. `pnpm --filter @memolabs-apps/visual-benchmark test` passes `Test Files 9 passed (9)` / `Tests 96 passed (96)`. The check is load bearing: appending a comment to `packages/target-vue/fixtures/field-workflow/fieldLogic.ts` made the suite report `Tests 1 failed | 95 passed (96)` at the digest assertion in `src/companion.test.ts`, and removing the line restored `Tests 96 passed (96)`.

## 4. Behaviour tests and clean platform builds

- [x] 4.1 Add deterministic behaviour tests over the moved rules unit (the next value, whether a save may proceed, what the save reports). Verify the test passes and fails when a rule is inverted.

  `packages/target-vue/src/field-logic.test.ts` imports `nextIn`, `canSave` and `saveOutcome` from `../fixtures/field-workflow/fieldLogic.js`, the same relative import the fixture screen uses, and covers both boundary cases rather than the happy path: the next value advances, wraps at the end of the list, and starts at the first value when the current one is unknown, and the save rule refuses an empty or whitespace-only status while allowing a real one and reporting `error` or `saved` accordingly. `pnpm --filter @memolabs-apps/target-vue test` passes 21 tests in 3 files. The check is load bearing: inverting `canSave` to `status.trim().length === 0` made the suite report `Tests 2 failed | 19 passed (21)` at `src/field-logic.test.ts:25`, and restoring the rule returned it to 21 passing tests. `pnpm --filter @memolabs-apps/visual-benchmark test` then still passes 96 tests, which also proves the restored unit is the one the provenance record hashed.

- [x] 4.2 Bundle the assembled companion for both declared platforms from the clean generated output and assert the declared identifiers and the declared action wiring appear in each bundle. Verify the assembly prints both bundle sizes and reports the identifiers found in each.

  The assembly run in 3.1 built both bundles from the freshly generated screen: `ios: 6584 kB` and `android: 6604 kB`, then `ios: 15/15 testIDs present` and `android: 15/15 testIDs present` for the fifteen declared field-workflow identifiers. The script also asserts the moved action wiring itself, not only the identifiers: `nextIn` and `saveOutcome` had to appear in each bundle text or the run fails, so the shared behaviour travelling into the companion is checked as well as its screen.

## 5. Evidence and close

- [x] 5.1 Write `docs/evidence/vue-companion-assembly.md` naming the planner decisions, every file origin with its reason, the recorded generated screen hash, the bundle results and the statement that no subset extension was needed. Verify the document names each origin and `pnpm prettier --check` passes on it.

  `docs/evidence/vue-companion-assembly.md` names the command and the compiler that produced it (compiler 0.1.1, generated screen hash `3ae83ea130a3bdb80d17649cdf79f1bdf12a3881937e8870d8d3a9a525cf7e32`), the planner classification of every subject (`fieldLogic.ts` and `fieldRecords.ts` `shared`, both screens `native-replacement`, the `vue` dependency `portable` and named as carrying no file), and a table of every file in the companion with its origin and reason, including the two moved units with the source path and the sha256 they were copied at. It records that no subset extension was needed and why, the two bundle sizes with the identifier counts, the action wiring assertion, and a closing section stating that nothing here is a device result (the companion bundles but has not been driven on a device by this change) and that there is no visual-fidelity or conversion claim. `pnpm prettier --check docs/evidence/vue-companion-assembly.md` prints `All matched files use Prettier code style!`.

- [x] 5.2 Run `pnpm build`, `pnpm typecheck`, `pnpm test`, `pnpm lint`, `pnpm format:check`, `pnpm deps:check` and the assembly command. Verify every command exits 0.

  All seven commands exit 0 with the companion work in place: `pnpm build`, `pnpm typecheck`, `pnpm test`, `pnpm lint`, `pnpm format:check` and `pnpm deps:check` (logs `/tmp/p2-gate-<command>.log`), and `node scripts/build-field-workflow-companion.mjs` (log `/tmp/p2-assembly2.log`), which reassembled the companion and reprinted the same evidence: both units moved, `ios: 6584 kB` and `android: 6604 kB`, `ios: 15/15 testIDs present` and `android: 15/15 testIDs present`, and the provenance record written.
- [x] 5.3 Run `openspec validate vue-companion-source-to-target --strict` before implementation and before archival, then archive the change. Verify no task is marked complete until its stated command or artifact exists.

  `openspec validate vue-companion-source-to-target --strict` printed `Change 'vue-companion-source-to-target' is valid` before implementation and again before archival. `openspec archive vue-companion-source-to-target --yes` reported `Task status: 10/11 tasks`, applied the specification delta (`companion-provenance: create`, `+ 3 added`) and archived the change as `2026-09-22-vue-companion-source-to-target`; the one task still open at that moment is this archival task itself. Every other task was left unchecked until its stated command or artifact existed, and each note records the command output or the artifact it produced.
