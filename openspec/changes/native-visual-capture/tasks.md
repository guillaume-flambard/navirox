## 1. Share the fixture application helpers

- [x] 1.1 Move the packed-artifact, scaffold, compile-and-verify, install and single-runtime helpers out of `scripts/verify-records-screen.mjs` into `scripts/lib/fixture-app.mjs` and import them from that script. Verify `node scripts/verify-records-screen.mjs --keep` still exits 0 and prints the same manifest hash, single runtime assertion and bundle sizes as before the move.

  The helpers now live in `scripts/lib/fixture-app.mjs` (`step`, `assert`, `run`, `capture`, `sha256`, `requireBuild`, `newWorkspace`, `scaffold`, `publishablePackages`, `pack`, `installGeneratedScreen`, `consumeFromArtifacts`, `install`, `lintApp`, `assertInstalledFromArtifacts`, `installedCopies`, `assertSingleRuntime`, `bundle`, `assertTestIds`, `removeWorkspace`, plus the path constants) and `scripts/verify-records-screen.mjs` is a thin `main()` over them, with the same printed steps and the same `--keep` flag. `node scripts/verify-records-screen.mjs --keep` exits 0 and prints the unchanged evidence: `manifest 4e556ef30b20b25aa3d41209e26e4c4bf492cc2f750752760038704e0394002a compiler 0.1.1`, `5 inside the app`, `5 packages, one copy each`, `ios: 6576 kB`, `android: 6596 kB`, `ios: 9/9 testIDs present`, `android: 9/9 testIDs present`. The one number that moved is the packed artifact count, 31 to 32, because `packages/visual-benchmark` itself became a publishable package after the earlier transcript was recorded; the fixture application is otherwise byte-identical (81 files, same manifest hash).
- [x] 1.2 Add a test that a fixture application whose fresh compiler output differs from the emitted screen file is refused before anything is installed. Verify `pnpm --filter @memolabs-apps/visual-benchmark test` fails when the check is removed and passes when it is present.

  The rule is now `compileFixtureScreen` in `packages/visual-benchmark/src/fixture-screen.ts`, which compiles the web fixture through the target compiler entry, refuses a compilation with findings, refuses a compilation that reported no findings but emitted no code, and throws `StaleFixtureScreenError` when the fresh output does not hash-match the emitted file, so the caller never reaches the install step. `src/fixture-screen.test.ts` covers the four branches against a fake compiler module written into a temp directory (5 tests). Mutation check performed literally: with the hash comparison forced false, `pnpm --filter @memolabs-apps/visual-benchmark test` reported `2 failed | 38 passed` (the two stale-file assertions), and with the check restored it reports `Test Files 4 passed (4) / Tests 40 passed (40)`.

## 2. The native capture driver

- [x] 2.1 Implement `captureNativeDevice` in `packages/visual-benchmark/src/drivers.ts` as a real driver: it builds and installs the prepared application on the declared device profile, runs the capture test for one capture, and writes the screenshot to the requested path, with the process runner injectable for tests. Verify `pnpm --filter @memolabs-apps/visual-benchmark test` passes new unit tests for a produced capture, for a missing application binary and for an unreachable device.

  `captureNativeDevice` now takes a `NativeDriverOptions` with the prepared application directory, the built binary path, the device name, the scenario file and the artifact directory, plus an injectable `DeviceProcessRunner` (the default wraps `spawnSync`). It refuses a call with no prepared application, refuses a missing Detox install, runs `detox build --configuration ios.sim.debug|android.emu.debug` when the binary is absent and re-checks the binary afterwards, then runs `detox test` with the capture environment (`NAVIROX_PLATFORM`, `NAVIROX_DEVICE`, `NAVIROX_SCENARIO`, `NAVIROX_ARTIFACTS`, `NAVIROX_CAPTURE`, `NAVIROX_CAPTURE_TEST`), and fails with `CaptureUnavailableError` for a pipeline that never ran and `CaptureMissingError` for a run that produced nothing. `src/drivers.test.ts` adds 6 tests against a fake runner and a temp application directory: a produced capture runs build then test in order, an existing binary skips the build, a build that leaves no binary is refused, an unreachable device is refused with the exit status and stderr, a call with no prepared application is refused, and a missing Detox install is reported when no runner is injected. `pnpm --filter @memolabs-apps/visual-benchmark test` reports `Test Files 5 passed (5) / Tests 48 passed (48)`.
- [x] 2.2 Record the device profile and the compiler manifest hash in the run report and keep the unavailable reason for a capture that was not produced. Verify `run.test.ts` asserts the profile of a produced capture and the reason of an unavailable one, and that `pnpm --filter @memolabs-apps/visual-benchmark test` passes.

  `runScenario` takes an optional fourth argument, `ScenarioRunContext { screen?: { compilerVersion, manifestHash }, devices?: Partial<Record<NativePlatform, DeviceProfile>> }`; `CaptureOutcome` gains `devices` holding the profile only for a platform that produced a capture, and `ScenarioRunReport` gains `devices` (every declared profile, captured or not) and the optional `screen` revision, so a report can never look complete without naming the device and the compiler revision behind it. `run.test.ts` adds two tests: one asserts the produced iOS capture carries `{ platform: 'ios', deviceName: 'iPhone 17', osVersion: '26.5' }` while the absent Android capture carries the reason `capture 'rest' unavailable: the android emulator was not reachable` and no profile, and the report carries the screen revision; the other asserts that a caller who knows no profile gets empty `devices` and no `screen` rather than invented values. `pnpm --filter @memolabs-apps/visual-benchmark test` reports `Tests 48 passed (48)`.
- [x] 2.3 Write the device harness the driver installs into the prepared application: a Detox configuration whose device name comes from the environment, a jest configuration, a setup file and a capture test that reads the scenario path, the artifact directory and the platform from the environment, drives each capture's declared actions and writes one screenshot per capture. Verify a test asserts every harness file is written and that the capture test text names the declared actions and the five capture moments.

  The harness is `writeDeviceHarness` in `packages/visual-benchmark/src/device-harness.ts`, writing five files into `<app>/e2e`: `detox.config.js` (device from `NAVIROX_DEVICE` with the platform default, application name read from the generated `app.json` so the renamed iOS scheme follows, both `ios.sim.debug` and `android.emu.debug` configurations, `reversePorts: [8081]`), `jest.config.js` (Detox jest lifecycle, one worker, 120 s test timeout, ts-jest), `setup.ts`, `tsconfig.json` (extends the app config with `jest`, `detox`, `node` types) and `capture.test.ts`. The capture test takes the scenario, artifact directory, platform, capture key and root test identifier from the environment, refuses a moment outside the five labels, launches with synchronization off and re-enabled once the screen is up, drives each declared action (including `nth`) before the capture, and copies the path `device.takeScreenshot` returns to `<artifacts>/<key>.<platform>.png`, which is the exact path the driver checks. `src/device-harness.test.ts` (6 tests) asserts every harness file is written and non-empty, that the capture test text drives `capture.actions` and names all five moments plus every environment variable, that the Detox configuration takes the device from the environment, and that a stale harness file is overwritten. `pnpm --filter @memolabs-apps/visual-benchmark build` and `test` pass with `Test Files 6 passed (6) / Tests 54 passed (54)`. The harness dependencies (`detox`, `jest`, `jest-circus`, `ts-jest`, `@types/jest`) are exported as `DEVICE_HARNESS_DEPENDENCIES`; installing them belongs to the capture script, which owns the prepared application.

## 3. The capture script

- [x] 3.1 Write `packages/visual-benchmark/scripts/capture-records-native.mjs`: it prepares the fixture application, installs the harness, builds and runs the capture for one platform, asserts every declared capture exists, writes the run report under `docs/evidence/` and exits nonzero when a capture is missing. Verify `node packages/visual-benchmark/scripts/capture-records-native.mjs --platform ios` exits 0, that every declared `records-list` capture exists in the artifact directory as `<key>.ios.png`, and that the report names the device it ran on.

  `node packages/visual-benchmark/scripts/capture-records-native.mjs --platform ios --keep` exits 0 and writes `docs/evidence/records-native-capture-records-list-ios.json` with `device { platform: 'ios', deviceName: 'iPhone 17' }`, `labels ['rest']`, `labelsInOrder true`, `missing []`, one frame `rest.ios.png 82343 bytes fca074b12944bb6f3c9bac6eed8d067d5f5df3301f51ea6e4b5c343f2bc5abf8` beside the web capture, and the Android capture recorded unavailable with the reason that the run prepared one platform. The script packs the workspace, scaffolds the fixture application outside the checkout, compiles the emitted screen through `compileFixtureScreen` (which refuses a stale emitted file before anything is installed), installs the harness and its dependencies from the packed artifacts, asserts the single runtime copy, runs `pod install` for iOS, and starts the web harness so the browser capture is produced in the same run. Three defects had to be fixed before a capture existed, all recorded here because each one is a property of the pipeline rather than of the fixture: the synchronous device spawn needed a raised `maxBuffer` (`DEVICE_OUTPUT_LIMIT_BYTES`, an Xcode build prints tens of megabytes and the default buffer kills the child with `ENOBUFS` instead of reporting the build error); the prepared application's `node_modules/.bin` had to be added to the environment the driver hands to Detox, because Detox runs the harness `start` command through a shell and `react-native` was otherwise not found; and the CocoaPods step (`installIosPods`, shared with `scripts/e2e-scaffold.mjs`) had to run before the build, because the scaffolded application ships a `Podfile` and lockfile but no `Pods` directory.
- [x] 3.2 Capture the motion scenario on the same path so the five temporal labels are produced on a device as they already are on the web. Verify the run report lists `rest, first-meaningful, midpoint, settled, interrupted` in order and that coincident frames are reported with a shared hash rather than hidden.

  The same command run without `--scenario` captures both scenarios and writes `docs/evidence/records-native-capture-records-motion-ios.json`, which lists `rest, first-meaningful, midpoint, settled, interrupted` in order with `labelsInOrder true`, one frame per label (rest 82478 bytes, first-meaningful 92911, midpoint 90803), and `Coincident frames: midpoint and settled and interrupted` carrying the shared hash `e3fceffb00d6d9fc6fa6ddcdad9146be27f517466e0fbe5ec0d859cd6aa23cae`. The coincidence is reported rather than hidden because the fixture is a discrete state machine: the settled frame presses the already-selected row and the interrupted frame replaces a pending selection, so both land on the same state as midpoint, and the report names the frames that share a hash instead of presenting three states as three distinct measurements. Driving the declared actions on a device exposed two real defects. First, `element(by.id(...))` cannot address an identifier that appears more than once: Detox refuses a matcher that matches several views, so `first-meaningful` and `interrupted` (whose first action carries no `nth`) failed with `Multiple elements found` while `midpoint` and `settled` passed; the harness `target` helper now always indexes (`atIndex(action.nth ?? 0)`). Second, the generated screen crashed at mount on iOS with `Text string "Alpha" must be rendered inside a <Text>`, because the benchmark fixture put bare text inside a `pressable`, which the safe subset accepts and the compiler emits as code that throws on a device. That is a target compiler gap and it is outside this change's declared impact, so the fixture was corrected (`<span class="row-button-label">` and `<span class="retry-label">`) and the gap is recorded in the evidence document as a discovered defect with its reproduction instead of being absorbed here. Regenerating the emitted file moved the recorded manifest hash from `4e556ef30b20b25aa3d41209e26e4c4bf492cc2f750752760038704e0394002a` to `e18712f368212b2481a868a3e6fa3fd5e42a58449fc099e1f503648b7eab4c48`; `pnpm --filter @memolabs-apps/target-vue test` passes with `Tests 10 passed (10)`, since the fixture test recompiles the source and compares bytes and manifest.

## 4. Continuous integration

- [ ] 4.1 Add a capture job per platform to `.github/workflows/ci.yml` that prepares the fixture application, runs the capture for that platform and uploads the artifact directory and the run report, reusing the existing emulator setup for Android and the simulator setup for iOS. Verify the workflow parses and a dispatched run on a branch retains the captures and the report for both platforms.
- [ ] 4.2 Cache the Gradle distribution in the Android capture job so a transient distribution download cannot fail the job as it did twice in run 35618352008. Verify the job log reports a cache hit on a second run of the same job.

## 5. Evidence and documentation

- [x] 5.1 Write `docs/evidence/records-native-capture.md` with the device profiles, every capture file and its sha256, the compiler manifest hash, and the measured web-against-device differences. Verify the document names every capture and states in its opening line and in a closing section that it makes no visual-parity claim.

  The document opens by saying it is a capture report and that it makes no visual parity claim, because no scenario declares a cross-platform tolerance, and closes with a "What this does not claim" section that repeats it. It names the run that produced the captures (`node packages/visual-benchmark/scripts/capture-records-native.mjs --platform ios --workspace /tmp/navirox-capture-final`), the device profile `{ platform: 'ios', deviceName: 'iPhone 17' }`, the 1206 x 2622 device screen against the 390 x 844 browser viewport, the target compiler `@memolabs-apps/target-vue` 0.1.1 and the manifest hash `e18712f368212b2481a868a3e6fa3fd5e42a58449fc099e1f503648b7eab4c48`, and it records that the run recompiles the web fixture and refuses a stale emitted file before it installs anything. It lists every capture with its bytes and sha256: one `records-list` frame (`rest.ios.png`, 82864 bytes, `3cb6fb6a...`) and the five `records-motion` frames in order (`rest` 82864, `first-meaningful` 93240, and `midpoint`/`settled`/`interrupted` sharing 90617 and one hash), with the coincidence explained as a discrete state machine rather than presented as three measurements. It records the measured web-against-device comparison (`rest.web.png` 390 x 844 against `rest.ios.png` 1206 x 2622, normalized to a 118 x 256 grid, 27782 of 30208 pixels differing, unmasked ratio 0.9197, bounding box the whole frame, verdict `undeclared-differences`) and explains why the difference is expected while stating that the verdict is undeclared because no tolerance is declared, so no pass is reported and V2 stays not started. It records the compiler gap found by the device run (bare text inside a `pressable` accepted by the safe subset and thrown at mount), the four pipeline defects fixed, and the Android captures as unavailable rather than omitted. The capture bytes and hashes were refreshed from the final gate run, because an iOS screenshot is not byte-identical between runs (the status bar clock moves); the measurement is stable across those runs, which is why its numbers did not change.
- [x] 5.2 Update `docs/VISUAL-FIDELITY.md` so V1 is complete with the driver named and V2 still reads not started. Verify the gate table says V1 is complete, points at the evidence document, and does not claim any measured visual fidelity.

  The V1 row now reads "Complete: the web capture and the native capture driver are verified on the records fixture", names what the driver does (builds and installs the screen the target compiler emitted on the declared device profile, drives the scenario declared actions, records the device profile and the compiler manifest hash beside every capture) and points at `docs/evidence/records-native-capture.md`. The V2 row still reads "Not started" and now says why in the terms the comparison rule uses: the captures exist on a device, but no scenario declares a cross-platform tolerance, so the comparison stays a measurement and is reported as an undeclared difference rather than a pass. V3 and V4 are unchanged, and the paragraph below the table still reserves a measured-fidelity claim for V2 and a scoped adapter claim for V4, so nothing in the document claims measured visual fidelity.

## 6. Verify and close

- [x] 6.1 Run `pnpm build`, `pnpm typecheck`, `pnpm test`, `pnpm lint`,
      `pnpm format:check`, `pnpm deps:check` and
      `node packages/visual-benchmark/scripts/capture-records-native.mjs --platform ios`.
      Verify every command exits 0.

  Every command exits 0. The six workspace commands were run after the driver, the
  harness and the capture script were finished, and `format:check` covers the new
  evidence document and the CI workflow. The capture script was then run exactly as
  the task writes it, without the `--workspace` flag, and it captured both
  scenarios on the simulator and ended with `Done. Workspace: <path>` and no
  failure line, so a run that leaves its workspace to a temporary directory and
  deletes it works as well as a named one. One honest note about the bytes: an iOS
  screenshot is not byte-identical between runs, because the capture is taken
  immediately after the last declared tap and the status bar clock or a residual
  pressed state can still be on screen. The committed reports therefore record the
  numbers of the final kept run (`records-list` `rest.ios.png` 82864 bytes
  `3cb6fb6a...`, `records-motion` `rest` 82864, `first-meaningful` 93240, and
  `midpoint`/`settled`/`interrupted` sharing 90617 and one hash), which is also the
  run the evidence document describes. The normalized measurement did not move
  across any of the runs.

- [ ] 6.2 Run `openspec validate native-visual-capture --strict` before implementation and before archival, then archive the change. Verify no task is marked complete until its stated command or artifact exists.
